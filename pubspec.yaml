name: footboard
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.0
  flutter_bloc: ^7.0.0-nullsafety.3
  fluttertoast: 8.0.1-nullsafety.0
  get_it: ^6.0.0
  provider: ^5.0.0
  rxdart: ^0.26.0
  web_socket_channel: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.11.5
  # trigger with: `flutter pub run build_runner build --delete-conflicting-outputs`
  json_serializable: ^4.0.2
  pedantic: ^1.11.0

flutter:
  uses-material-design: true